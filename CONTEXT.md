# 领域术语表 — AI 体态监测与矫正

> 本文件只定义领域概念，不含实现细节。实现决策见 `docs/adr/`。

## 体态

**体态 (Posture)**：人体在坐着时各部位（头、颈、躯干、肩）相对于重力方向的空间排列。本系统中特指学生伏案学习时的坐姿。

**不良坐姿 (Poor Sitting Posture)**：体态偏离用户个人基线超过阈值的持续状态，可能对颈椎、腰椎或视力造成压力。瞬时调整（前倾拿东西）不计为不良坐姿。

## 检测指标

**头部前倾角 (Head Forward Angle)**：头部中心相对于双肩中点的水平偏移量。正值表示头部前探，对应颈椎应力。

**躯干前倾角 (Torso Forward Angle)**：肩-髋连线与垂直方向的夹角。正值表示弯腰驼背，对应腰椎应力。仅侧面模式可精确测量。

**面部距离估算 (Face Distance Estimation)**：通过面部关键点包围框的面积，相对估算人脸与屏幕的距离。不追求绝对精度，只判断"比基线近了多少"。

**久坐时长 (Sitting Duration)**：用户连续处于摄像头前且未起身离开的累计时间。不依赖姿态检测，纯定时器计算。

## 校准与基线

**基线 (Baseline)**：用户在校准时被记录的"正确坐姿"各项角度值。后续所有检测都是相对基线的偏移量，而非绝对角度。

**校准 (Calibration)**：用户首次打开系统时，摆出正确坐姿并手动确认的操作。系统此时锁定各角度作为基线。

**手动校准 (Manual Calibration)**：由用户主动触发、需 30 秒完成的基线建立方式。区别于"自动统计基线"（从前 N 分钟数据取中位数），手动校准保证基线本身是正确的。

## 姿态评分

**姿态评分 (Posture Score)**：综合各项指标偏移量计算得出的 0-100 分数值。80+ 良好（绿）、60-79 警告（黄）、<60 不良（红）。

**不良持续时长 (Poor Posture Duration)**：姿态评分低于阈值（如 60）的连续秒数。达到 5 秒才触发提醒，过滤瞬时调整。

**冷却期 (Cooldown)**：提醒触发后的 60 秒静默期，期间不重复弹窗，避免连续轰炸。

## 视力保护

**20-20-20 法则 (20-20-20 Rule)**：眼科通用建议——持续注视屏幕 20 分钟后，看 20 英尺（约 6 米）外的物体 20 秒，缓解睫状肌疲劳。本系统在检测到持续注视满 20 分钟时触发提醒。

**注视时长 (Gaze Duration)**：用户面部持续朝向摄像头/屏幕方向的累计时间。用于触发 20-20-20 法则提醒。

## 交互

**骨架叠加 (Skeleton Overlay)**：在摄像头预览画面上实时绘制 MediaPipe 检测到的人体骨架线，颜色随姿态评分变化（绿/黄/红），让用户直观看到 AI 在测量什么。

**提醒 (Alert)**：检测到不良坐姿持续达阈值后弹出的视觉提示。区别于"通知"（Notification，指系统级弹窗），提醒是页面内浮层。
